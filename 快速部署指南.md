# 🚀 快速部署指南

## 📊 服务器规格推荐

### 💰 **预算友好型**（适合测试/小规模使用）
- **CPU**: 2核心
- **内存**: 4GB
- **存储**: 40GB SSD
- **月费**: ¥50-100（阿里云/腾讯云）

### ⭐ **推荐配置**（适合正式使用）
- **CPU**: 4核心
- **内存**: 8GB
- **存储**: 80GB SSD
- **月费**: ¥200-400（阿里云/腾讯云）

### 🚀 **高性能型**（大规模使用）
- **CPU**: 8核心
- **内存**: 16GB
- **存储**: 160GB SSD
- **月费**: ¥600-1000（阿里云/腾讯云）

### VPS 服务商推荐
1. **阿里云 ECS** - 适合中国用户，稳定可靠
2. **腾讯云 CVM** - 新用户优惠多
3. **华为云** - 价格实惠
4. **Vultr** - 国际用户首选
5. **DigitalOcean** - 简单易用

---

## 🎯 最简单的部署方法（推荐）

### 步骤 1: 购买服务器
1. 选择 Ubuntu 22.04 LTS 系统
2. 配置至少 4GB 内存
3. 记录服务器 IP 地址和 root 密码

### 步骤 2: 连接服务器
```powershell
# Windows PowerShell 中执行
ssh root@你的服务器IP
```

### 步骤 3: 上传后端代码

**方法 A: 使用 SCP（推荐）**
```powershell
# 在本地 Windows PowerShell 中，先打包
cd F:\Python-Project\FL_CC_Production
Compress-Archive -Path backend\* -DestinationPath backend.zip

# 上传到服务器
scp backend.zip root@你的服务器IP:/root/
```

**方法 B: 手动上传**
- 使用 FileZilla 或 WinSCP 工具
- 连接到服务器
- 上传整个 backend 文件夹

### 步骤 4: 在服务器上解压
```bash
# 在服务器上执行
mkdir -p /home/translation
cd /home/translation
unzip /root/backend.zip -d backend
cd backend
```

### 步骤 5: 运行自动部署脚本
```bash
# 给脚本执行权限
chmod +x server_deploy.sh

# 运行脚本（会自动安装所有依赖）
./server_deploy.sh
```

### 步骤 6: 配置 API 密钥
脚本会提示您输入：
- OpenAI API Key
- 百度翻译 API Key
- 百度翻译 Secret Key

### 步骤 7: 等待完成
脚本会自动完成：
- ✅ 安装系统依赖
- ✅ 创建虚拟环境
- ✅ 安装 Python 包
- ✅ 配置 Nginx
- ✅ 配置 Supervisor
- ✅ 启动服务

### 步骤 8: 记录您的 API 地址
部署完成后，脚本会显示：
```
服务器 IP: 123.456.789.0
API 地址: http://123.456.789.0
```

### 步骤 9: 更新前端配置
```powershell
# 在本地编辑
cd F:\Python-Project\FL_CC_Production\frontend
```

编辑 `.env.production` 文件：
```env
REACT_APP_API_URL=http://你的服务器IP
```

然后重新部署前端：
```powershell
npm run deploy
```

---

## 🎉 完成！

现在您的应用已经完全部署完成：
- ✅ 前端: https://zac-chen-2024.github.io/FileTranslationFrontend
- ✅ 后端: http://你的服务器IP

---

## 📝 常用操作命令

### 查看服务状态
```bash
sudo supervisorctl status translation-platform
```

### 查看日志
```bash
# 应用日志
tail -f /home/translation/backend/logs/error.log

# Supervisor 日志
tail -f /home/translation/backend/logs/supervisor.log
```

### 重启服务
```bash
sudo supervisorctl restart translation-platform
```

### 更新代码
```bash
cd /home/translation/backend

# 上传新文件后
sudo supervisorctl restart translation-platform
```

---

## ⚠️ 常见问题

### Q1: 部署后无法访问？
**A**: 检查防火墙是否开放 80 端口
```bash
sudo ufw allow 80/tcp
sudo ufw reload
```

### Q2: 502 Bad Gateway？
**A**: 检查 Gunicorn 服务是否运行
```bash
sudo supervisorctl status translation-platform
sudo supervisorctl restart translation-platform
```

### Q3: 文件上传失败？
**A**: 检查上传目录权限
```bash
sudo chown -R translation:translation /home/translation/backend/uploads
chmod 755 /home/translation/backend/uploads
```

### Q4: 内存不足？
**A**: 添加 Swap 空间
```bash
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

### Q5: 想使用域名？
**A**: 修改 Nginx 配置
```bash
sudo vim /etc/nginx/sites-available/translation-platform

# 修改 server_name 行
server_name your-domain.com;

# 重启 Nginx
sudo systemctl restart nginx
```

### Q6: 想配置 HTTPS？
**A**: 使用 Let's Encrypt 免费证书
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

---

## 📞 需要帮助？

1. **查看详细文档**: `DEPLOYMENT_GUIDE.md`
2. **检查日志文件**: `/home/translation/backend/logs/`
3. **验证服务状态**: `sudo supervisorctl status`

---

## 🎯 下一步优化

部署完成后，建议进行以下优化：

1. ✅ 配置域名
2. ✅ 启用 HTTPS
3. ✅ 设置自动备份
4. ✅ 配置监控告警
5. ✅ 优化性能参数

祝部署顺利！🎉

